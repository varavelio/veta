// Package dirs resolves Veta runtime directories.
//
// The package owns the Veta home directory and cache locations. It intentionally
// does not download themes, read project files, load configuration, render
// content, or write build output.
package dirs
