// Package scaffold creates starter Veta projects on disk.
//
// The package owns project initialization files only. It must not parse CLI
// arguments, run builds, resolve themes, or render pages.
package scaffold
