// Package config loads Veta's tool configuration.
//
// The package only owns settings that change how the Veta binary behaves. Site
// content, theme data, SEO metadata, navigation, and other user-facing values
// belong in the project's data directory, not in veta.yaml.
package config
