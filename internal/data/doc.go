// Package data loads Veta's global site data.
//
// The package owns parsing files from the project's data directory and exposing
// them as named JSON-compatible values. It intentionally does not know about
// pages, templates, filters, components, themes, Markdown, or output files.
package data
